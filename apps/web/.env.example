# Base URL for the backend CRUD API. Defaults to /api (proxied to :3000 in dev).
VITE_API_URL=/api
# Set to 'mock' to bypass the Better Auth login gate (matches backend
# APP_MODE=mock). Used for LAN preview without Google sign-in. Leave unset for
# normal auth.
VITE_APP_MODE=
