{
  "name": "spend-tracker-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.1.1",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm -r --parallel dev",
    "migrate": "pnpm --filter backend migrate"
  }
}
